export const mostrar_texto = (texto_ingresado, texto_mostrar) => {
    texto_mostrar.textContent = texto_ingresado.value;
}

export const cambiar_fondo = (parrafo) => {
    parrafo.style.backgroundColor = "#4dff4d";
}
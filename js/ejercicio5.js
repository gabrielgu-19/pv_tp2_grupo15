const horas = 3, minutos = 35;
const horas_segundos = horas * 3600;
const minutos_segundos = minutos * 60;

console.log(`Horas y minutos: ${horas} horas y ${minutos} minutos`);
console.log(`Equivalente en segundos: ${horas_segundos + minutos_segundos} segundos`);